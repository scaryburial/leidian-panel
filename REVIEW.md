# 审查说明

本分支基于 3x-ui，代码审查请关注：

- 是否影响既有入站/客户端/订阅/路由逻辑。
- 是否引入对外网联（本项目默认去除公网 IP 探测、更新检查等外联）。
- 前端改动：类型检查与构建是否通过（`npm run typecheck` / `npm run build`）。
- 后端改动：`go build ./...` 是否通过；涉及数据库结构时是否有迁移。
- 配置生成（Xray 模板）改动是否会导致内核启动失败（可用 `xray run -test -c config.json` 校验）。
