# 雷电面板（ui3344）项目说明与注意事项

> 本面板基于开源项目 3x-ui 深度定制，更名为「雷电面板」，命令/服务/路径统一为 `ui3344`。
> 本文档说明：已完成的内容、未完成的内容、以及安装与使用前必须了解的注意事项。

---

## 一、已完成的功能

1. **品牌更名**
   - 显示名统一为「雷电面板」：登录页、侧边栏、浏览器标题、登录页 title、PWA 名称、命令面板、两步验证发行方。
   - 去除了侧边栏的 GitHub / 文档 / 捐赠链接，以及设置页里的 GitHub 文档链接。

2. **界面背景定制**
   - 登录页：雷电将军背景图（带可读性遮罩）。
   - 面板内所有页面：狐妖小红娘（涂山容容/红红/苏苏等）6 张图，每 6 秒交叉淡入淡出轮换。

3. **命令 / 服务 / 路径改名 → `ui3344`**
   - 终端管理命令：`ui3344`（安装到 `/usr/bin/ui3344`）。
   - 服务名：`ui3344`（systemd `ui3344.service`；Alpine openrc `ui3344`）。
   - 目录：`/usr/local/ui3344`、`/etc/ui3344`（数据库 `ui3344.db`）、`/var/log/ui3344`。
   - 环境文件：`/etc/default|conf.d|sysconfig/ui3344`。
   - 安装包/资产名：`ui3344-linux-<arch>.tar.gz`。

4. **终端交互菜单中文化**
   - `ui3344` 主菜单、全部子菜单（SSL/Cloudflare、IP 限制、防火墙、SSH 转发、PostgreSQL、BBR、日志管理等）、提示与状态消息均已翻译为中文。

5. **默认配置**
   - 登录账号 / 密码：`3344` / `3344`
   - 面板端口：`33441`
   - 安全入口（webBasePath）：`/ui3344/`
   - 说明：安装脚本已取消“检测到默认凭据就随机重置”的逻辑，保证默认值固定生效。

6. **隐私：去除会泄露信息的外联**
   - 关闭公网 IP 探测（不再请求 ipify/icanhazip/ipinfo 等）。
   - 关闭面板更新检查、Xray 版本列表的联网拉取。
   - 保留（功能需要）：另见“注意事项·隐私”。

7. **Xray 内核离线切换**
   - 面板内置 3 个代表性内核版本，可在“Xray 版本切换”里离线切换，无需联网：
     - `v26.9.9`（默认，推荐）、`v25.12.2`、`v25.6.7`
   - 切换时自动补齐缺失的 `geoip.dat` / `geosite.dat`。

8. **客户端 IP 归属地（离线 ip2region）**
   - 客户端列表新增「在线 IP / 归属地」列（对在线客户端显示 IP + 省市运营商）。
   - 客户端 IP 弹窗中，每个 IP 后显示归属地。
   - 新增接口 `POST /panel/api/server/ipGeo`，可一键查询任意 IPv4 归属地。
   - 数据为离线 ip2region 库，运行时不联网；IPv4 精确，IPv6 不支持（显示为空）。

9. **安装方式（自建发布包）**
   - 提供本地安装包 `ui3344-linux-amd64.tar.gz`：含二进制、`ui3344.sh`、服务文件、默认内核与 geo 文件、`install.sh`（本地安装，不访问上游）。

---

## 二、未完成 / 暂缓的功能

1. **每客户端「最大并发连接数」限制 —— 未做**
   - 原因：Xray 内核没有“每用户并发连接数”计数（只提供在线用户与其源 IP）。硬做需解析访问日志近似统计，不精确。
   - 现状：可用“每客户端 IP/设备数限制”（`limitIp`，精确到 N 个来源 IP）替代。
2. **每客户端「上下行速率限制」—— 未做**
   - 原因：Xray 内核**没有原生限速**；`bufferSize` 不是限速。真正限速需 Linux `tc`，复杂且同账号多设备时不精确。
3. **`install.sh` / `update.sh` 的英文用法方框 —— 未中文化**
   - 且 `update.sh` 仍指向上游地址，**自建包升级请不要用它**，建议用本地包重装覆盖（数据保留）。
4. **`.github/workflows`、`deploy/` 目录 —— 仍是 `x-ui` 名称**
   - 仅影响 CI/云端部署脚本，不影响运行时。

---

## 三、注意事项与风险

### 1. 安全（最重要）
- **默认账号密码 `3344/3344` 是弱口令，极易被爆破，务必在首次登录后立刻改强。**
- 建议同时：更改“安全入口”路径、配置 SSL 证书，或把面板绑定 `127.0.0.1` 并通过 SSH 隧道/反向代理访问。

### 2. 默认值与端口
- 端口 `33441`、入口 `/ui3344/`、账号密码 `3344/3344`。若 `33441` 被占用，安装后可在 `ui3344` 菜单里改端口。

### 3. 隐私边界（重要，请知悉）
- **已去除**：公网 IP 探测、面板更新检查、Xray 版本列表联网拉取、界面里的上游链接。
- **仍会联网（功能需要，保留了）**：
  - “Xray 版本切换”时，若切换到**内置之外**的版本，会从 XTLS/Xray-core 官方 GitHub 下载该内核；
  - “更新 Geo 文件”会从上游规则仓库下载；
  - 订阅/节点/Telegram 等按你的配置联网。
- 若不希望任何上述联网，请不要使用“切换非内置内核 / 更新 Geo”等操作。

### 4. 归属地精度
- ip2region 对国内省 / 市较准，但**动态 IP、基站 IP、移动网络**仍可能偏差；运营商信息较可靠。任何 IP 库都无法 100% 准确。

### 5. 已知现象（非功能问题）
- 因为默认安全入口改成了 `/ui3344/`，仓库里的 `go test` 用例 `TestRouteRegistryContract` 会把路由视为带 `/ui3344` 前缀而报“未注册”**失败**。**真机运行完全正常**，可忽略；如需该测试变绿需另行调整测试预期。

### 6. 运行环境
- 目标：Debian / Ubuntu 等 systemd 环境（或 Alpine openrc）。需要 **root** 权限。
- 安装包体积较大（约 128MB，含内置内核与前端资源）。

### 7. 数据库与迁移
- 数据库默认位于 `/etc/ui3344/ui3344.db`（SQLite）。**这与原版 3x-ui 的 `/etc/x-ui/x-ui.db` 不同**。
- 若你之前装过原版 3x-ui，本面板**不会**自动接管其数据；需要迁移请另行处理（可用面板的导入功能）。

---

## 四、卸载
```
systemctl stop ui3344
systemctl disable ui3344
rm -f /etc/systemd/system/ui3344.service
rm -f /usr/bin/ui3344
rm -rf /usr/local/ui3344 /etc/ui3344 /var/log/ui3344
systemctl daemon-reload
```

---

## v2 更新
- 面板**透明度进一步提高**（背景图更明显）。
- **左侧边栏科技化美化**（玻璃拟态/渐变品牌/圆角菜单/选中发光）。
- **安装时自动创建 9 个预设协议**（VLESS / VMess / SS2022 各 速度/安全/综合），装完即可用，无需手动添加。
