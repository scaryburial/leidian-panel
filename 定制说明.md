# 定制说明（需求清单 · 变更历史 · 凭据）

> 本文记录：你提出的每一项要求、对应的实现情况、以及默认凭据与变更概览。
> 详细功能与注意事项见同目录的《雷电面板-项目说明与注意事项.md》，安装与使用见《雷电面板-使用说明.md》。

---

## 一、需求清单与实现情况（按提出顺序）

| # | 你的要求 | 实现情况 |
|---|----------|----------|
| 1 | 登录页背景换成雷电将军的图（要好看） | ✅ 登录页使用雷电将军背景图 + 可读性遮罩 |
| 2 | 进入面板后用狐妖小红娘动漫图轮换（涂山容容/红红等） | ✅ 6 张图，每 6 秒交叉淡入淡出，全页面通用 |
| 3 | 面板改名「雷电面板」 | ✅ 登录页/侧边栏/标题/PWA/命令面板/2FA 发行方 |
| 4 | 去掉 GitHub 仓库链接等上游身份；检查并杜绝隐私收集 | ✅ 移除侧边栏/设置页外链；关闭公网 IP 探测、更新检查、版本列表联网 |
| 5 | 默认账号密码 3344、端口 33441、安全入口 /ui3344/ | ✅ 已设为默认（安装脚本不再随机重置） |
| 6 | 终端命令 xui → ui3344（只保留 ui3344） | ✅ 命令/服务/路径/文件名统一为 ui3344 |
| 7 | SSH 终端交互界面翻译成中文 | ✅ 主菜单+各子菜单+提示/状态全部中文化 |
| 8 | 路径/文件名/服务名也改成 ui3344（能保证安全的前提下） | ✅ 已全改并做真机安装验证 |
| 9 | Xray 版本切换列表不要为空；几个代表性内核离线可用 | ✅ 内置 v26.9.9 / v25.12.2 / v25.6.7，离线切换 |
| 10 | 面板上显示连接用户 IP 及归属地（要精确，别把上海判成湖南）；一键查 | ✅ 客户端列表列 + IP 弹窗归属地 + 一键查接口（离线 ip2region） |
| 11 | 每客户端「限速」与「最大并发连接数」 | ⏸ 暂缓：Xray 无原生支持（详见注意事项文档） |
| 12 | 安装方式选 B（自建发布包 + 本地安装脚本） | ✅ 已生成 ui3344-linux-amd64.tar.gz 与本地 install.sh |
| 13 | 把改动提交到本地 git，并整理需求与变更历史 | ✅ 见本文件与 git 提交记录 |
| 14 | 生成安装包 + 说明/使用文档 | ✅ 见随包文档 |

---

## 二、默认凭据（请务必尽快修改）

- 登录账号：`3344`
- 登录密码：`3344`  ← **弱口令，首次登录后立刻改强**
- 面板端口：`33441`
- 安全入口：`/ui3344/`
- 数据库：`/etc/ui3344/ui3344.db`（SQLite，注意与原版 3x-ui 的 `/etc/x-ui/x-ui.db` 不同）

---

## 三、变更概览（按主题）

1. **品牌与背景**
   - `frontend/src/pages/login/*`、`frontend/src/layouts/*`、`frontend/public/manifest.webmanifest`、`frontend/login.html`
   - 新增 `frontend/src/assets/`（背景图）、`frontend/src/layouts/PanelBackground.*`（轮换背景）、`frontend/src/styles/page-shell.css`
2. **隐私去外联**
   - `internal/web/service/server.go`（公网 IP 探测、Xray 版本列表）、`internal/web/service/panel/panel.go`（面板更新检查）、前端外链
3. **默认值**
   - `internal/web/service/setting.go`（端口/入口）、`internal/database/db.go`、`main.go`（默认凭据判断）、`install.sh`
4. **改名 ui3344（命令/服务/路径/文件名）**
   - `internal/config/*`、`internal/logger`、`internal/web/service/*`、`internal/web/controller/*`
   - `ui3344.sh`、`ui3344.service.{debian,arch,rhel}`、`ui3344.rc`、`install.sh`、`update.sh`、`Dockerfile`、`DockerEntrypoint.sh`、`docker-compose.yml`
5. **终端菜单中文化**
   - `ui3344.sh`
6. **内核离线切换**
   - 新增 `internal/web/service/cores/`（内置内核 zip）、`xray_cores.go`、`xray_versions.go`；`internal/web/service/server.go`（UpdateXray）
7. **IP 归属地**
   - 新增 `internal/ipgeo/`（内置 ip2region.xdb）
   - `internal/web/service/inbound_node_ips.go`、`internal/web/controller/server.go`
   - 前端 `frontend/src/lib/clients/ip-log.ts`、`ClientsPage.tsx`、`ClientInfoModal.tsx`、`ClientFormModal.tsx`、`frontend/src/pages/api-docs/endpoints.ts`、13 个语言文件
8. **安装方式**
   - 新增 `packaging/install.sh`、`packaging/README.md`、`build-release.sh`

（完整逐文件差异见 git 提交记录。）

---

## 四、未完成 / 暂缓

- 每客户端「限速」（Xray 无原生，需 tc）
- 每客户端「最大并发连接数」（Xray 无连接计数；可用 limitIp 替代）
- `install.sh`/`update.sh` 的英文用法方框未中文化；`update.sh` 仍指向上游，升级请用本地包重装
- `.github/workflows`、`deploy/` 仍为 x-ui 名称（非运行时）

---

## 五、相关文档

- 《雷电面板-项目说明与注意事项.md》
- 《雷电面板-使用说明.md》
