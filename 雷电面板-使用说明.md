# 雷电面板（ui3344）使用说明

---

## 一、安装

### 1. 合并安装包分片（若收到的是分片文件）
本包因体积（约 128MB）被拆成 `part.0`、`part.1` 两个分片。合并：
```
cat ui3344-linux-amd64.tar.gz.part.0 ui3344-linux-amd64.tar.gz.part.1 > ui3344-linux-amd64.tar.gz
```
校验（应与随包提供的 sha256 一致）：
```
sha256sum ui3344-linux-amd64.tar.gz
# 期望：c7ce611149530f3730982a5f8f526fa5a29155573f85f13b58068938e20283d3
```

### 2. 解压并安装（需 root）
```
tar xzf ui3344-linux-amd64.tar.gz
cd ui3344
bash install.sh
```
安装脚本会：安装到 `/usr/local/ui3344`、写入 `/usr/bin/ui3344`、注册并启动 `ui3344.service`（systemd）或 openrc 服务。全程本地操作，不联网。

### 3. 访问
```
http://<服务器IP>:33441/ui3344/
```
- 默认账号 / 密码：`3344` / `3344`
- **首次登录后请立刻修改账号密码，并建议配置 SSL / 改安全入口。**

---

## 二、管理命令 `ui3344`

在 SSH 终端输入 `ui3344` 打开中文交互菜单：

```
0.  退出脚本          1.  安装           2.  更新
3.  更新到开发版      4.  更新菜单       5.  旧版本
6.  卸载              7.  重置用户名密码 8.  重置网页根路径
9.  重置设置          10. 修改端口       11. 查看当前设置
12. 启动              13. 停止           14. 重启
15. 重启 Xray         16. 查看状态       17. 日志管理
18. 开机自启          19. 取消自启
20. SSL 证书管理      21. Cloudflare SSL 证书
22. IP 限制管理       23. 防火墙管理
24. SSH 端口转发管理  25. PostgreSQL 管理
26. 开启 BBR          27. 更新 Geo 文件  28. Ookla 测速
```

常用：
- `ui3344`                —— 打开菜单
- `ui3344 start|stop|restart|status` —— 启停/状态
- `ui3344 settings`       —— 当前设置
- `ui3344 log` / `ui3344 banlog` —— 日志 / 封禁日志

---

## 三、面板功能要点

- **界面背景**：登录页为雷电将军图；进入面板后为狐妖小红娘 6 图轮换（每 6 秒）。
- **客户端 IP / 归属地**：
  - 客户端列表有「IP / 归属地」列：**在线与离线客户端都会显示**，离线客户端保留其**最后一次在线的 IP 与归属地**（变暗表示离线）。
  - 内网 / 保留地址显示「内网」，查不到的显示「未知」。
  - 点客户端查看 IP 时，会显示每个 IP 的省市运营商。
  - 一键查 IP：接口 `POST /panel/api/server/ipGeo`（表单 `ip=1.2.3.4`）。
- **分享 / 订阅（把链接发给客户）**：
  - 客户端每行左侧**第一个（链接图标）按钮 = 「分享 / 订阅」**，点开即可一键复制 / 扫码导入。
  - 弹窗含：**原始订阅**（小火箭 Shadowrocket / v2rayN 等）、**订阅 (JSON)**、**订阅 (Clash)**（Clash / Mihomo / 安卓“猫”等），以及单节点 `vless:// / vmess:// / ss://` 链接与二维码。
  - **每个客户端都有独立订阅链接**，直接发给对应客户即可。
  - 订阅服务端口 **2096**（需在安全组/防火墙放行）；链接形如 `http://<服务器IP>:2096/<随机路径>/<subId>`。
  - **内置离线分流规则**（含在 Clash 订阅里）：广告拦截 → 国内直连（`.cn` / `GEOIP,CN`）→ 其余走代理。小火箭是节点订阅，分流在其 App 内设置。
- **中转（出口代理）**：新增「中转」菜单，可把客户端流量的**出口**改到上游 **SOCKS5 / HTTP** 代理：
  - 生效范围支持 **全部客户端 / 指定客户端（邮箱）/ 指定入站**；
  - 「测试连通」按钮直接显示经上游出网的 IP；
  - 关闭开关即恢复直连。**客户端节点链接不变**，中转在服务端完成（写入 Xray 出站 + 路由，tag=`ui3344-relay`）。
- **Xray 版本切换**（概览页的版本入口）：可在内置的 `v26.9.9 / v25.12.2 / v25.6.7` 之间**离线**切换。
- **更新 Geo 文件**：菜单 27，或面板内的 Geo 更新（会联网到规则仓库）。
- **IP / 设备数限制**：在客户端/入站的 `limitIp` 设置每客户端最大来源 IP 数（依赖 Fail2ban）。

---

## 四、安全建议（务必执行）

1. **改强账号密码**（菜单 7，或设置页）。
2. **更换安全入口**（菜单 8，或设置页），避免固定 `/ui3344/` 被扫描。
3. **启用 SSL**（菜单 20/21，或设置页自定义证书路径）。
4. 或：把面板监听改为 `127.0.0.1`，通过 SSH 隧道访问：
   ```
   ssh -L 33441:127.0.0.1:33441 root@<服务器IP>
   ```
   然后本地浏览器访问 `http://127.0.0.1:33441/ui3344/`。

---

## 五、升级与卸载

### 升级（用本地包重装，数据保留）
1. 解压新的 `ui3344-linux-amd64.tar.gz`
2. 进入目录执行 `bash install.sh`（脚本会停旧服务、覆盖程序、保留 `/etc/ui3344` 数据）

> 注意：**不要使用 `ui3344 update` 菜单**——它指向上游地址，本定制版不适用。

### 卸载
```
systemctl stop ui3344
systemctl disable ui3344
rm -f /etc/systemd/system/ui3344.service
rm -f /usr/bin/ui3344
rm -rf /usr/local/ui3344 /etc/ui3344 /var/log/ui3344
systemctl daemon-reload
```

---

## 六、常见问题

- **打不开面板**：确认服务在运行 `systemctl status ui3344`；确认端口 `33441` 已放行；确认访问路径带 `/ui3344/`。
- **登录报错/密码忘了**：菜单 7 重置用户名密码。
- **归属地不准**：ip2region 对国内省市较准，动态/基站 IP 可能偏差，属正常。
- **IPv6 归属地为空**：内置库为 IPv4 库，IPv6 不显示归属地。
- **想换绑定的内核版本**：概览页 Xray 版本切换，或菜单相关项。
- **归属地随时间不准**：ip2region 是离线快照库，运营商重分配网段后会偏。解决办法：用新版 `ip2region.xdb` 覆盖 `internal/ipgeo/ip2region.xdb` 后重新构建即可（无需改代码）。
- **订阅链接打不开**：确认已放行端口 **2096**；链接需含服务器 IP + 随机路径 + 该客户端的 subId。

---

## v2 更新
- 面板**透明度进一步提高**（背景图更明显）。
- **左侧边栏科技化美化**（玻璃拟态/渐变品牌/圆角菜单/选中发光）。
- **安装时自动创建 9 个预设协议**（VLESS / VMess / SS2022 各 速度/安全/综合），装完即可用，无需手动添加。

---

## v3 更新
- **归属地列离线也显示**：保留客户端**最后一次在线**的 IP 与归属地。
- **客户端新增「分享 / 订阅」按钮**：弹窗含 原始 / JSON / Clash 订阅与二维码，一键发链接给客户（适配小火箭、Clash、v2rayN）。
- **订阅服务开箱即用**：开启 Clash / JSON 订阅格式，**内置离线分流规则**（广告拦截 + 国内直连 + 其余代理）。
- **每个预设客户端分配独立 subId**（独立订阅链接）。
- 订阅端口 **2096**（记得在安全组放行）。

---

## v4 更新
- 新增侧边栏「中转」菜单：**服务端出口中转**（上游 SOCKS5 / HTTP），支持 全部 / 指定客户端 / 指定入站，含**连通性测试**。
- 中转配置写入 Xray 出站 + 路由（tag=`ui3344-relay`），关闭开关即自动移除、恢复直连。
